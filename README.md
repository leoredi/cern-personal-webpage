Federico Leo Redi — personal academic website for LHCb-focused research.
Static pages live in `site/` with shared assets under `assets/` and content notes in `content/`.
Key links: [ORCID](https://orcid.org/0000-0001-9728-8984), [INSPIRE](https://inspirehep.net/authors/Federico.Redi.1), [University of Bergamo](https://www.unibg.it/).
Serve locally with `python -m http.server` and browse to `http://localhost:8000/site/`.
Deploy by running the GitHub Pages workflow or copying `site/` and `assets/` to your static host root.
Update recent publications with `python scripts/fetch_inspire.py` (requires `requests`).
